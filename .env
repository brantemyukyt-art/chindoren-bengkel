# MySQL standalone configuration
DB_USERNAME=root
DB_PASSWORD=
DB_HOST=localhost
DB_NAME=bengkel_db

# Combined SQLAlchemy URI for MySQL with PyMySQL driver
# SQLALCHEMY_DATABASE_URI=mysql+pymysql://${DB_USERNAME}:${DB_PASSWORD}@${DB_HOST}/${DB_NAME}
SQLALCHEMY_DATABASE_URI=sqlite:///bengkel.db